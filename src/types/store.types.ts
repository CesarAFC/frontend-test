export type Product = {
    id: string;
    name: string;
    price: number;
    description: string;
    imageSrc: string
}

export type CardInformation = {
    card: string
    expirationDate: string
    securityCode: string
    holderName: string
  }